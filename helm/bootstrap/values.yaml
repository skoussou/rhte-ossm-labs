# Default values for bootstrap.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

user:
  count: 2
  prefix: user

ossm:
  git:
    url: http://unknown:3000/gitops/myapp
    revision: main
    path: helm/ossm
  values:
    domain: apps.cluster-guid.sandbox.opentlc.com
    myKey: myValue
